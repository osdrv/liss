#include <stdio.h>
#include "vm.h"

void initVM(VM* vm) {
    // Initialize VM state
    (void)vm; // Suppress unused variable warning for now
    DEBUG_LOG("Initializing VM...");
}

void freeVM(VM* vm) {
    // Free VM resources
    (void)vm; // Suppress unused variable warning for now
    DEBUG_LOG("Freeing VM...");
}

